spring.application.name=pos
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.session.timeout=1d

# Production (Aiven MySQL)
spring.datasource.url=jdbc:mysql://mysql-a1bcafa-commonbrew-pos.c.aivencloud.com:12667/commonbrew_pos?useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_MfjGjgrQHHlpsXHSFIC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Point to the Aiven CA certificate (adjust path as needed)
# spring.datasource.hikari.data-source-properties.sslMode=VERIFY_CA
# spring.datasource.hikari.data-source-properties.sslCa=classpath:ca.pem

# Alternative if storing in filesystem:
# spring.datasource.hikari.data-source-properties.sslCa=/path/to/aiven_ca.pem

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Other configurations remain the same...
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.security.user.name=admin
spring.security.user.password=admin

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Redis connection
spring.cache.type=simple
# spring.data.redis.host=redis
# spring.data.redis.port=6379
# spring.data.redis.time-to-live=600000 # 10 minutes cache
