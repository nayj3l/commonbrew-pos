spring.application.name=pos
server.port=${PORT:8080}
server.address=0.0.0.0

# local
# spring.datasource.url=jdbc:mysql://localhost:3306/commonbrew_pos
# spring.datasource.username=root
# spring.datasource.password=

# Production (Aiven MySQL)
spring.datasource.url=jdbc:mysql://mysql-a1bcafa-commonbrew-pos.c.aivencloud.com:12667/commonbrew_pos?sslMode=VERIFY_CA&enabledTLSProtocols=TLSv1.2
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_MfjGjgrQHHlpsXHSFIC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Tell connector where the CA file is (filesystem path inside container)
spring.datasource.hikari.data-source-properties.sslCa=/app/ca.pem
spring.datasource.hikari.data-source-properties.sslMode=VERIFY_CA

# JPA
spring.jpa.hibernate.ddl-auto=none
# Optional: remove explicit dialect (Hibernate can auto detect). If you keep it:
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Thymeleaf & security
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.security.user.name=admin
spring.security.user.password=admin

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true